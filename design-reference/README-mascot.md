# Chereka mascot

`mascot-3d-exploration.png` is the source style sheet (nightcap character, 12 states).

**Production:** soft 3D PNGs in `assets/mascot/`, rendered via `MoonFace` (`src/components/brand/MoonFace.tsx`). PRODUCT_SPEC §18.8 allows this for personality moments; the flat `MoonMark` remains the logo / app icon.

Ship individual state exports from the illustrator when available — current PNGs are interim renders matching the sheet. Prefer transparent backgrounds and no held props when the UI already labels the state.
